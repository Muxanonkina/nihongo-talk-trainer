# nihongo-talk-trainer
Web app for Japanese conversation training

# User Progress
User progress is stored in the database and can be accessed through the API.
Отчет о проделанной работе: Full-Stack History Screen

**Общий статус:** В процессе.

**Блокер:** Миграция базы данных не может быть завершена из-за проблемы с подключением к базе данных.

---

### Проделанная работа:

#### Backend:
-   **Модель данных:**
    -   ✅ Схема Prisma (`prisma/schema.prisma`) обновлена: в модель `UserProgress` добавлено поле `messages` для хранения истории переписки.
-   **API:**
    -   ✅ Создан новый модуль `userprogress`.
    -   ✅ Созданы роуты (`userprogress.routes.ts`) для получения истории (`GET /api/userprogress` и `GET /api/userprogress/:id`).
    -   ✅ Создан контроллер (`userprogress.controller.ts`) для обработки запросов.
    -   ✅ Создан сервис (`userprogress.service.ts`) для взаимодействия с базой данных.
    -   ✅ Новые роуты зарегистрированы в главном файле приложения (`app.ts`).
    -   ✅ Сервис обновлен для включения связанной информации о диалоге (`dialog`) в ответы API.

#### Frontend:
-   **Страница истории:**
    -   ✅ Создана страница `history/page.tsx` для отображения списка пройденных диалогов.

---

### Оставшиеся задачи:

#### Backend:
-   **Миграция базы данных:**
    -   ⏳ Необходимо решить проблему с подключением к базе данных и запустить миграцию.

#### Frontend:
-   **Страница детального просмотра:**
    -   ⏳ Создать страницу `history/[id]/page.tsx` для отображения полной истории переписки.
-   **Сохранение истории:**
    -   ⏳ Добавить логику на страницу тренировки (`training/[id]/page.tsx`) для сохранения истории переписки в `UserProgress`.

Как только проблема с базой данных будет решена, я продолжу выполнение оставшихся задач.
